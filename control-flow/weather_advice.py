#App to provide clothing recommendations to user based on user input
#Define weather conditions within an if statement

current_weather = str(input("What's the weather like today?"))
if current_weather = str("sunny"):
    